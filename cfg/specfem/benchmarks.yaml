_engine: matrix
_nb_agents: 1
_name: matrix

run:
  - current

expe:
  current:
    driver: Specfem3D

    settings:
      machine: laptop, desktop
      nex: 16, 32, 64
      threads: 1, 2, 3, 4, 5, 6, 7, 8

    scripts:

scripts:
  setup:

  teardown:

  display:
    before:
    after:
